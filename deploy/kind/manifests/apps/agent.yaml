# =============================================================================
# The agent service.
#
# Holds the LangGraph run state in memory, so a paused (approval-required) run
# can be resumed via POST /resume. It fetches a JWT-SVID from the Workload API
# and exchanges the rep's token per call — it holds no static credential.
#
# Model calls go ONLY through the LLM gateway, over mTLS with the agent's
# X.509-SVID. The agent has no provider key and no model endpoint of its own.
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent
  namespace: agent-platform
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
  namespace: agent-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent
  template:
    metadata:
      labels:
        app: agent
    spec:
      serviceAccountName: agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: agent
          image: agent-platform/agent:demo
          imagePullPolicy: IfNotPresent
          env:
            - name: KC_ISSUER
              value: http://keycloak:8080/realms/agent-platform
            - name: SPIFFE_SOCKET
              value: unix:///run/spire/sockets/agent.sock
            - name: SPIFFE_BUNDLE
              value: /run/spire/bundle/bundle.crt
            - name: AGENT_SPIFFE_ID
              value: spiffe://acme.com/ns/agent-platform/sa/agent
            - name: TOOL_SERVER_URL
              value: http://tools:8000
            - name: APPROVALS_URL
              value: http://api:8080
            - name: AUDIT_URL
              value: http://api:8080
            # The agent reaches the model ONLY through the gateway, over mTLS.
            - name: LLM_GATEWAY_URL
              value: https://gateway:8443
            # Used only by the demo/attack scripts (they stand in for alice's
            # and the manager's apps when minting tokens). From a Secret, never
            # committed.
            - name: DEMO_CLI_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: DEMO_CLI_SECRET
            - name: MANAGER_CLI_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: MANAGER_CLI_SECRET
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
          ports:
            - containerPort: 8081
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /run/spire/sockets
              readOnly: true
            - name: spire-bundle
              mountPath: /run/spire/bundle
              readOnly: true
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: spiffe-workload-api
          hostPath:
            path: /run/spire/sockets
            type: Directory
        - name: spire-bundle
          configMap:
            name: spire-bundle
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: agent-platform
spec:
  selector:
    app: agent
  ports:
    - name: http
      port: 8081
      targetPort: 8081
